node_modules
/src/terminal/

# Main-site build output (regenerated by `bun run build` / CI).
# dist/terminal is committed separately from the terminal-portfolio repo.
/dist/index.html
/dist/alt.*.min.css
/dist/assets/
/dist/robots.txt
/dist/sitemap.xml
